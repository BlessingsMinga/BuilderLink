import { router } from 'expo-router';
import { ChevronLeft, Star } from 'lucide-react-native';
import { useState } from 'react';
import { Alert, Pressable, Text, TextInput, View } from 'react-native';
import { AppButton } from '@/shared/components/app-button';
import { Screen } from '@/shared/components/screen';
const categories = ['Communication', 'Work quality', 'Time management', 'Value for money', 'Professionalism'];
export default function Review() { const [rating, setRating] = useState(5); const [comment, setComment] = useState(''); const submit = () => { if (!comment.trim()) return Alert.alert('Add a review', 'Please write a short review to help future customers.'); Alert.alert('Review submitted', 'Thanks. Your review will be published after moderation.', [{ text: 'Done', onPress: () => router.replace('/(tabs)') }]); }; return <Screen><Pressable onPress={() => router.back()} className="h-11 w-11 items-center justify-center rounded-2xl bg-white"><ChevronLeft color="#1E293B" /></Pressable><Text className="mt-6 text-3xl font-extrabold text-ink">Rate your experience</Text><Text className="mt-2 text-slate-500">How was working with Thoko Mbewe?</Text><View className="mt-8 items-center rounded-3xl bg-white p-6"><Text className="text-lg font-bold text-ink">Overall rating</Text><View className="mt-4 flex-row gap-2">{[1,2,3,4,5].map((value) => <Pressable key={value} onPress={() => setRating(value)}><Star size={34} color="#F97316" fill={value <= rating ? '#F97316' : 'transparent'} /></Pressable>)}</View><Text className="mt-3 font-semibold text-slate-500">{rating} out of 5</Text></View><Text className="mt-8 text-lg font-bold text-ink">Rate the details</Text><View className="mt-3 gap-2">{categories.map((item) => <View key={item} className="flex-row items-center justify-between rounded-2xl bg-white p-4"><Text className="font-semibold text-ink">{item}</Text><Text className="font-bold text-brand">Excellent</Text></View>)}</View><Text className="mt-8 text-lg font-bold text-ink">Your review</Text><TextInput value={comment} onChangeText={setComment} multiline className="mt-3 h-32 rounded-2xl bg-white px-4 pt-4 text-base" placeholder="Share what went well and what could improve." /><Text className="mt-3 text-sm text-slate-500">Reviews are moderated to keep BuilderLink fair and useful.</Text><View className="my-7"><AppButton label="Submit review" onPress={submit} /></View></Screen>; }
