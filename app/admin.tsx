import { router } from 'expo-router';
import { AlertTriangle, BarChart3, ChevronLeft, ClipboardCheck, Users } from 'lucide-react-native';
import { Pressable, Text, View } from 'react-native';
import { Screen } from '@/shared/components/screen';
const cards = [{ label: 'Pending verifications', value: '12', icon: ClipboardCheck, color: '#F97316' }, { label: 'Open reports', value: '3', icon: AlertTriangle, color: '#EF4444' }, { label: 'Active users', value: '1,284', icon: Users, color: '#16A34A' }, { label: 'Monthly revenue', value: 'MWK 4.2m', icon: BarChart3, color: '#2563EB' }];
export default function Admin() { return <Screen><Pressable onPress={() => router.back()} className="h-11 w-11 items-center justify-center rounded-2xl bg-white"><ChevronLeft color="#1E293B" /></Pressable><Text className="mt-6 text-3xl font-extrabold text-ink">Admin dashboard</Text><Text className="mt-2 text-slate-500">Platform operations at a glance.</Text><View className="mt-7 flex-row flex-wrap gap-3">{cards.map((card) => { const Icon = card.icon; return <View key={card.label} className="w-[47%] rounded-3xl bg-white p-4"><Icon size={22} color={card.color} /><Text className="mt-5 text-2xl font-extrabold text-ink">{card.value}</Text><Text className="mt-1 text-sm text-slate-500">{card.label}</Text></View>; })}</View><Text className="mt-8 text-lg font-bold text-ink">Priority queue</Text><View className="mt-3 gap-3"><Row title="Verify builder documents" detail="12 submissions awaiting review" action="Review" /><Row title="Moderate customer report" detail="Possible price misrepresentation" action="Investigate" /><Row title="Review flagged content" detail="2 reviews require attention" action="Moderate" /></View><View className="mt-8 rounded-3xl bg-orange-100 p-5"><Text className="font-bold text-orange-800">Security boundary</Text><Text className="mt-1 leading-5 text-orange-700">Approval, suspension, payout, and role changes run through a protected server-side admin service—not this client screen.</Text></View></Screen>; }
function Row({ title, detail, action }: { title: string; detail: string; action: string }) { return <View className="rounded-2xl bg-white p-4"><Text className="font-bold text-ink">{title}</Text><Text className="mt-1 text-sm text-slate-500">{detail}</Text><Text className="mt-3 font-semibold text-brand">{action}</Text></View>; }
