import { useLocalSearchParams, router } from 'expo-router'; import { Text, TextInput, View } from 'react-native'; import { AppButton } from '@/shared/components/app-button'; import { Screen } from '@/shared/components/screen';
export default function RegisterRole() { const { role } = useLocalSearchParams<{ role: 'customer' | 'builder' }>(); const label = role === 'builder' ? 'professional' : 'customer'; return <Screen><Text className="text-3xl font-extrabold text-ink">Create your account</Text><Text className="mt-2 text-base text-slate-500">Join as a {label}. You can complete the rest later.</Text><View className="mt-9 gap-4"><TextInput className="h-14 rounded-2xl border border-slate-200 bg-white px-4 text-base" placeholder="Full name" /><TextInput className="h-14 rounded-2xl border border-slate-200 bg-white px-4 text-base" placeholder="+265 phone number" keyboardType="phone-pad" /><TextInput className="h-14 rounded-2xl border border-slate-200 bg-white px-4 text-base" placeholder="Email address" keyboardType="email-address" /><AppButton label="Create account" onPress={() => router.replace('/(tabs)')} /></View></Screen>; }
