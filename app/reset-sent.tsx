import { useLocalSearchParams, router } from 'expo-router'; import { MailCheck } from 'lucide-react-native'; import { Text, View } from 'react-native'; import { AppButton } from '@/shared/components/app-button'; import { Screen } from '@/shared/components/screen';
export default function ResetSent() { const { email } = useLocalSearchParams<{ email: string }>(); return <Screen><View className="flex-1 items-center justify-center"><View className="h-20 w-20 items-center justify-center rounded-3xl bg-orange-100"><MailCheck size={38} color="#F97316" /></View><Text className="mt-7 text-center text-3xl font-extrabold text-ink">Check your inbox</Text><Text className="mt-3 text-center text-base leading-6 text-slate-500">If an account exists for {email}, you’ll receive a password reset link shortly.</Text><View className="mt-9 w-full"><AppButton label="Return to sign in" onPress={() => router.replace('/login')} /></View></View></Screen>; }
