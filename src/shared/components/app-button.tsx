import { ActivityIndicator, Pressable, Text } from 'react-native';
type Props = { label: string; onPress?: () => void; variant?: 'primary' | 'outline'; loading?: boolean; disabled?: boolean };
export function AppButton({ label, onPress, variant = 'primary', loading = false, disabled = false }: Props) { const base = 'h-14 items-center justify-center rounded-2xl px-5'; const isDisabled = disabled || loading; return <Pressable accessibilityRole="button" accessibilityState={{ disabled: isDisabled, busy: loading }} disabled={isDisabled} onPress={onPress} className={`${base} ${variant === 'primary' ? 'bg-brand' : 'border border-slate-200 bg-white'} ${isDisabled ? 'opacity-60' : ''}`}>{loading ? <ActivityIndicator color={variant === 'primary' ? 'white' : '#F97316'} /> : <Text className={`text-base font-bold ${variant === 'primary' ? 'text-white' : 'text-ink'}`}>{label}</Text>}</Pressable>; }
