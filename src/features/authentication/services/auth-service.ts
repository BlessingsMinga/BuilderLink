import { createUserWithEmailAndPassword, sendEmailVerification, sendPasswordResetEmail, signInWithEmailAndPassword } from 'firebase/auth';
import type { ForgotPasswordInput, RegistrationInput, SignInInput } from '../schemas/auth-schemas';
import { firebaseAuth, isFirebaseConfigured } from '@/shared/services/firebase';
import type { AuthSession, UserRole } from '@/shared/types';
const requireAuth = () => { if (!firebaseAuth || !isFirebaseConfigured) throw new Error('Firebase has not been configured. Add the EXPO_PUBLIC_FIREBASE_* values to .env.'); return firebaseAuth; };
const session = (id: string, email: string, role: UserRole): AuthSession => ({ userId: id, email, role });
export const authService = { async signIn(input: SignInInput): Promise<AuthSession> { const auth = requireAuth(); if (!input.identifier.includes('@')) throw new Error('Sign in with your email address until phone authentication is configured.'); const credential = await signInWithEmailAndPassword(auth, input.identifier.trim(), input.password); return session(credential.user.uid, credential.user.email ?? input.identifier, 'customer'); }, async register(input: RegistrationInput, role: UserRole): Promise<AuthSession> { const auth = requireAuth(); const credential = await createUserWithEmailAndPassword(auth, input.email.trim(), input.password); await sendEmailVerification(credential.user); return session(credential.user.uid, input.email, role); }, async requestPasswordReset(input: ForgotPasswordInput): Promise<void> { await sendPasswordResetEmail(requireAuth(), input.email.trim()); }, async resendVerification(): Promise<void> { const user = requireAuth().currentUser; if (!user) throw new Error('Sign in again before requesting another verification email.'); await sendEmailVerification(user); } };
